#2. Write a program that uses random.choice() to randomly select a student from a list and display:

import random

students = ["Farooq", "Zakir", "Vineeth"]

selected = random.choice(students)

print("The selected student for presentation is:", selected)